import { Button } from "@/components/ui/button"

export default function ApplyButton() {
    // Falta implementar la función del botón, por ahora solo está el diseño
  return (
        <Button 
            variant="default"
            className="px-12 py-6 w-full text-base"
        >
            Aplicar
        </Button>
  )
}
